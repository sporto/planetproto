// ... you code here ...

claim(robot.parts, __);
claim(vehicle.parts, __);
claim(robot.capabilities, __);
claim(vehicle.capabilities, __);

// ... more code here

claim(robot.parts, __);
claim(vehicle.parts, __);
claim(robot.capabilities, __);
claim(vehicle.capabilities, __);

module.exports = {
	machine: machine,
	vehicle:    vehicle,
	robot:    robot
}
